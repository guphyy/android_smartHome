<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.shixun">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DigitalTime"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FunctionActivity" />
        <activity android:name=".Light" />
        <activity android:name=".TimerActivity" />
        <activity android:name=".Clock" />
        <activity android:name=".ZuceActivity" />
        <activity android:name=".Temperature" />
        <activity
            android:name=".SaveInfo"
            tools:ignore="Instantiatable" />
        <activity android:name=".Wifi_function" />
        <activity android:name=".FuncTcpClient" />
        <activity android:name=".FuncTcpServer" />
        <activity android:name=".MyCounter" />
    </application>

</manifest>